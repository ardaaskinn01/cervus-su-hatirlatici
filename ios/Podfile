# Podfile for Cervus Water App (Fixed Path) 💧🚀🦾

platform :ios, '14.0'

# Flutter generated dependency plugin info
flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # Not: GeneratedPluginRegistrant.rb artık otomatik olarak Flutter araçları tarafından bağlanır.
  # Ancak özel bir yol gerekiyorsa standardı takip etmeliyiz. 👇🎯
  target 'RunnerTests' do
    inherit! :search_paths
  end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    
    # 🕵️ Kritik Ayarlar: Deployment Target ve Swift Sürümü 👇🚀
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '14.0'
      config.build_settings['GCC_PREPROCESSOR_DEFINITIONS'] ||= ['$(inherited)', 'PERMISSION_NOTIFICATIONS=1']
      # Google Mobile Ads gereği static framework kullanımı için (gerekirse)
      # config.build_settings['BUILD_LIBRARY_FOR_DISTRIBUTION'] = 'YES'
    end
  end
end
